import { config } from '../config';

export const DB_USER = config.db.user;
export const DB_PASSWORD = config.db.password;
export const DB_NAME = config.db.name;
export const DB_HOST = config.db.host;
export const DB_PORT = config.db.port;
